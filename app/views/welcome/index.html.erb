<% if flash[:partial] %>
    <% partial = flash[:partial].to_s %>

    <%= render partial %>
<% end %>
<div class="page-title">Evan & Crystal</div>
<div class="date">April 20, 2015</div>
<div class="location">Costa Rica</div>

<% if session[:logged_in] %>
    <%= link_to 'RSVP', new_rsvp_path(@rsvp) %>
<% else %>
    <form method="post" action="/login">
      <input name="authenticity_token" value="<%= form_authenticity_token %>" type="hidden">
      <input type="text" name="password" placeholder="Enter Password">
      <input type="submit" value="Login">
    </form>
<% end %>


<%= render 'carousel' %>

<div class="quote-bar">“You don't love someone for their looks, or their clothes, or for
  their fancy car, but because they sing a song only you can hear.” ~ Oscar Wild
</div>